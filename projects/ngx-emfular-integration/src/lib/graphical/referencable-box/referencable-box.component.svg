<svg:g [attr.id]="referencable.$gId">
    <g rectangleG
       [attr.id]="referencable.$gId + '_main'"
       [position]="position"
       [color]="color">
    </g>
    <!-- class name -->
    <g text-area-svg
       [text]="referencable.getEClass()"
       [x]="position.x"
       [y]="position.y"
       [w]="position.w"
       [h]="position.h">
    </g>

    <!-- containers -->
    @for (container of referencable.$treeChildren;
        track referencable.$gId + '_' + container.referenceName
            ; let i = $index) {

        @let containerKey = referencable.$gId + '_' + container.referenceName;
        @let containerAnchorBox = GraphicalHelper.computeChildBBox(
            i,
            referencable.$treeChildren.length,
            position
        );

        <!-- reference anchor -->
        <g rectangleG
           [attr.id]="containerKey + '_ref'"
           [position]="containerAnchorBox"
           color="#ede679">
        </g>
        <!-- reference label -->
        <g text-area-svg
           [text]="container.referenceName"
           [x]="containerAnchorBox.x"
           [y]="containerAnchorBox.y"
           [w]="containerAnchorBox.w"
           [h]="containerAnchorBox.h">
        </g>

        <!-- arrow from main box to reference -->
        <g arrowElems
           [startGID]="referencable.$gId"
           startSuffix="_main"
           [endGID]="containerKey"
           endSuffix="_ref">
        </g>

        <!-- real children inside this reference -->
        @let realChildren = GraphicalHelper.getAsList(container);

        @for (elem of realChildren; track elem.$gId; let j = $index) {

            @let elemBox = GraphicalHelper.computeChildBBox(
                j,
                realChildren.length,
                containerAnchorBox
            );
            <g referencable-box
               [referencable]="elem"
               [position]="elemBox"
            >
            </g>
            <!-- arrow from reference to child box -->
            <g arrowElems
               [startGID]="containerKey"
               startSuffix="_ref"
               [endGID]="elem.$gId"
               endSuffix="_main">
            </g>
        }
    }
</svg:g>
